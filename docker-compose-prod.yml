version: "3"
services:
  nginx:
    image: wonohchoi/deploy-test:nginx
    ports:
      - "80:80"
    volumes:
      - ./nginx/nginx.conf:/etc/nginx/nginx.conf:ro
  nextjs:
    image: wonohchoi/deploy-test:nextjs
    ports:
      - "3000:3000"
